[project]
name = "quietnotes"
version = "0.1.0"
description = "A tiny notes command for the terminal"
requires-python = ">=3.11"

[project.scripts]
qn = "quietnotes.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
